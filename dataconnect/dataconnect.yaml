specVersion: "v1beta"
serviceId: "pmate"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pmate-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
